"""Temporary placeholder.
Webhook processing is intentionally disabled because deposits are scanned on-demand
and by background polling in main.py.
"""

if __name__ == "__main__":
    print("Webhook receiver disabled. Use blockchain scanner in main.py")
